// Global test setup — imported before every test suite by Vitest
// Extends the built-in expect with jest-dom matchers like:
//   .toBeInTheDocument(), .toHaveValue(), .toBeDisabled(), etc.
import '@testing-library/jest-dom';
