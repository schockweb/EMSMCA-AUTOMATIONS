# Environment variables — NEVER commit these
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Crew-form journey session bundle — holds a live crew token and portal grant.
# Generated by backend/crew_session_bootstrap.py; never commit it.
session.json
# PDF benchmark sample — carries a live ADMIN token.
pdfbench*.json
hunt.json
fault-hunt-report.json
